import { ExpiredSizePipe } from './expired-size.pipe';

describe('ExpiredSizePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpiredSizePipe();
    expect(pipe).toBeTruthy();
  });
});
