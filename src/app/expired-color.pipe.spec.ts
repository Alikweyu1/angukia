import { ExpiredColorPipe } from './expired-color.pipe';

describe('ExpiredColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpiredColorPipe();
    expect(pipe).toBeTruthy();
  });
});
